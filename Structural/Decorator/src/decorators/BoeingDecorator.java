package decorators;

import interfaces.IAircraftD;

/**
 * The Decorator Pattern requires an abstract decorator class that implements
 * the same abstract interface for the object being wrapped.
 */

public abstract class BoeingDecorator implements IAircraftD {}
